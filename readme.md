## Если планируем использовать в докере поменять значения в .env

### Комментируем

PG_ADDRESS=localhost

### Эту строку раскоментируем (Удалить image образ который уже собран! - (Docker->Images) paylalservice-app)

#PG_ADDRESS=paylal_db